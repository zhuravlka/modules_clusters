color_sch.R

#!‘ункции:
count.correlation.count.array  - берет  название коррел€ционной статистики  и 
список списков, сдержащий результаты подсчета коррел€ции
возвращает матрицу, содержащую значени€ только по вывбранной статистике

если в пивальювской статистики содержитс€ знак "" кавычек, то надо замен€ть значение в матрице
на ноль( делаем так, возвращаетс€ всегда NA, если такое случаетс€, соответственно 
замен€ютс€ NA в готовой матрице на нули)

heatmap.and.save - берет три матрицы дл€ трех типов кластеров и название коррел€ционной статистики
рисует и сохран€ет в файлах соответствующие heatmap'ы

remake.matrix- сюда отправл€ем матрицы, по которым
мы смотрим пивальювские значени€  статистик, (дополнительно на вход, матрицы с 1-0 TRUE-FALSE, 
соответственно указателей на  то в левом они конце распределени€ или нет), если не в левом , то вычитаем из 1 
, возвращаем переделанную матрицу

heatmap.and.save.pvalue-
....

#!–абота скрипта:

1) √рузим три RData, в каждой подчет с одним из трех типов кластеров

≈сть две статистики не пивальвские 'jaccard.measure''relative.distances.ecdf.area.correlation'
» три пивальювские 'jaccard.measure.p.value' 'scaled.absolute.min.distance.sum.p.value' 'projection.test.p.value'
и ещЄ две pvalue_unsigned - 'relative.distances.ecdf.deviation.area.p.value' и 'relative.distances.ks.p.value'

2) ƒл€ первых двух последовательно делаем так:
-считаем три матрицы с помощью функции count.correlation.count.array, 
-затем передаем эти матрицы в heatmap.and.save , в результате чего получаем три сохраненные картинки по соответствущему
типу коррел€ционной статистики

3) ƒл€ тех, в которых есть p-value, нужно учесть некоторые преобразовани€
при построении heatmap'а

4) 








